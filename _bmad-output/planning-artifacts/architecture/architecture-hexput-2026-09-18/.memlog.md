---
scope: Hexput v2 daemon architecture -- whole system covered by the v2 PRD
purpose: build-substrate + a to-be-chosen companion document (thesis solution-design writeup or deck, undecided; user said no preference, revisit at Finalize)
altitude: initiative
updated: 2026-09-18T10:39
---

- (decision) Paradigm confirmed with user: Hexagonal / Ports & Adapters at the transport boundary (UDS, Named Pipe, TCP+TLS, WebSocket as adapters into one internal RPC/message port, per PRD FR-9's identical-behavior-across-transports requirement) combined with an Actor model for the runtime core (Backend connections and Plugins as actors with owned state and mailboxes) -- fits the stateful, event-driven Plugin model (FR-17..FR-23) and the non-blocking requirement (FR-16) better than a plain layered/service architecture would.
- (decision) Session state (Plugin registrations, Global Variables, per-backend Config from FR-1/FR-3) is owned by a Session object that outlives the transient connection actor -- confirmed by user. A Session has a lifetime/TTL: after a disconnect with no reconnect within that TTL, the Session (and its Plugins/state) is torn down. This refines PRD FR-21's 'disconnect-without-reconnect' teardown trigger from immediate to TTL-based.
- (decision) User introduced a new concept: a system-wide, file-based daemon config (e.g. /etc/hexput/config.toml or similar system config directory), holding operational/daemon-level settings including at least the Session TTL default. This is DIFFERENT in scope from the PRD's per-backend Config (Glossary: 'Per-connection settings... Not a static file', FR-1: 'no file-based configuration path... for per-backend settings') -- that decision stands unchanged. But PRD Non-Goals SS5 currently states a blanket 'Hexput will not ship or require a static configuration file for the daemon' with no per-backend scoping, which now contradicts this new system config file. Flagging as a PRD wording gap to fix: the Non-Goal should be scoped to per-backend/business-logic config, not operational/daemon-level config.
- (decision) System config file (e.g. /etc/hexput/config.toml) scope confirmed as ALL daemon-level operational settings: transport bind addresses/ports, TLS certificate paths, log level, Session TTL default -- not just TTL. This resolves PRD OQ-4 (does config cover TLS settings) with a nuance: yes, but via this separate system config, not the per-connection/per-backend Config object, which stays non-file-based as originally decided.
- (direction) User approved fixing the PRD directly: narrow SS5 Non-Goals wording to scope it to per-backend/business-logic config (matching Glossary/FR-1's original intent), and add the System Config concept as a new PRD element (Glossary term + FR under SS4.1 or a new NFR).
- (decision) Confirmed: Global Variables are NOT actor-mailbox-owned state -- they live in a separate shared concurrent store (DashMap-like), reachable both by ordered/priority handlers (via the Plugin actor's sequential path) and by async=true handlers (spawned outside the actor's ordering, touching the store directly). The Plugin actor itself owns identity/event-routing/priority-ordering, not the Global Variable values.
- (decision) New requirement surfaced: Global Variables need a configurable BEHAVIOR/lifetime strategy per-variable, separate from the locking strategy (FR-20): 'forever' (default, persists for Session lifetime), 'ttl: <duration>' (value expires/resets after duration since last write), 'separate_each_trigger' (each Event invocation gets an independent, non-shared value despite being declared global), 'based on a key' (variable holds independent values partitioned by a key -- exact key source not yet specified by user, flagging as open question). This is PRD-level scope (FR-25), not just an implementation note -- routing it to the PRD.
- (decision) Confirmed: capability checks (FR-6/7) and Resource Budget enforcement (FR-8) must go through ONE shared enforcement point used by all three execution paths (Direct, Cached, Plugin Event handlers) -- not three independent implementations. This is AD-worthy: prevents the Plugin path silently drifting from the Script path's security/budget guarantees.
- (version) Web-verified current stable crate versions (2026-09-18): tokio 1.53.1, tokio-tungstenite 0.29.0, rustls 0.23.45 (TLS for TCP transport), rmp-serde 1.3.1 + serde 1.0.229 (MessagePack wire format), dashmap 6.2.1 (Global Variable store), moka 0.12.16 (AST Cache), criterion 0.8.2 (benchmarking), tracing 0.1.44 (structured logging). All previously named in PRD addendum without pinned versions -- now pinned for the Stack section.
- (version) Re-verified against crates.io API directly (my earlier tokio-tungstenite check was wrong): tokio-tungstenite is 0.30.0, not 0.29.0 -- fixing Stack table. moka 0.12.16 and rustls 0.23.45 independently re-confirmed correct despite rubric walker's conflicting claim (which appears to have used stale data). Rust stable pinned to 1.98.1 (2024 edition, released 2026-09-03) per web search -- was previously unpinned.
- (event) Reviewer Gate: lint_spine.py clean. Rubric walker: not-yet-clean (C-1 deployment/environments dimension entirely silent; H-1 FR-13 reconnect-credential validation point undecided; H-2 FR-11 health/metrics structural placement undecided; stack version issues, resolved above). Adversarial lens: NOT CLEAR, 7 divergences (D1-D7), all closed by tightening AD-2/AD-3/AD-4/AD-5 rule text and fixing the ER diagram's Session:Connection cardinality (was wrongly 0-or-1, contradicts FR-2's multi-connection requirement).
- (event) Applied all Reviewer Gate fixes: D1 (AD-4 gained explicit teardown(plugin_id) contract, session/ as sole caller), D2 (AD-3 gained live budget-handle-for-async-tasks clause), D3 (ER diagram cardinality fixed to 0..N, AD-2 gained per-Connection request/response correlation + no-broadcast rule -- my call, not separately re-confirmed with user, flagged for their review), D4 (AD-4 clarified 'owns ordering' means metadata only, actual sequencing is a per-(Plugin,Event) task off the mailbox), D5 (AD-5 gained 'session/ is the single live Config source, no snapshotting' rule), D6 (AD-4 lock granularity tightened to per-(top-level-key, partition-key)), D7 (AD-3 gained 'Global Variable access is intrinsic state, not a Registered Function, exempt from FR-6/7' clause). Rubric C-1/H-1/H-2 fixed: new AD-7 (System Config discovery precedence) + Deferred entry for the rest of deployment/environments; AD-2 gained FR-13 credential-validation-point rule; AD-1 gained FR-11 health/metrics placement (served via Port, pre-init-gate). Stack fixed: tokio-tungstenite corrected 0.29.0->0.30.0 (my earlier check was wrong), Rust pinned to 1.98.1 (2024 edition). lint_spine.py clean after all edits.
- (decision) D3 confirmed by user: no broadcast across Connections, Connections are isolated from each other -- matches my proposed resolution. Refinement: Session's TTL countdown only starts once ZERO Connections remain attached (not on every individual disconnect while other Connections of the same Session are still live) -- while at least one Connection is attached, the Session is actively alive with no TTL clock running. Companion artifact: user declined for now, spine alone is sufficient deliverable.
- (event) spine finalized
- (event) Post-finalize refinement using installed Rust guideline skills (m07-concurrency, m09-domain, unsafe-checker) per user request: (1) clarified the 'unsafe/lock-free' Global Variable strategy is implementable entirely in safe Rust (DashMap's own optimistic/finer-grained access, no literal unsafe blocks) and is unrelated to the Security NFR's 'no unsafe in VM path' rule -- prevents future confusion; (2) tightened AD-4 so priority-sequenced and async-spawned handlers both call one shared handler-invocation function in exec/ (capability/budget/GV-access setup shared, not duplicated) -- addresses user's 'benzer noktalar paylasimali' request; (3) added a Locking discipline convention (never hold a lock across .await, m07's anti-pattern) to Consistency Conventions; (4) added a domain-modeling note to Design Paradigm: Session owns Plugin lifecycle only, Plugin is its own consistency boundary for event dispatch (m09-domain aggregate-boundary guidance). lint_spine.py clean after edits.
- (change) Post-final amendment during sprint planning (2026-09-18): FR-26 (optional static check) needed a structural home. Added `check/` to the Structural Seed as a twelfth module and AD-8 to bind it. AD-8's job is preventing the three submission paths (Direct Execution, CodeRegister, Plugin registration) from each growing a partial checker that disagrees with the others -- the same drift AD-3 prevents for enforcement. Key rules: `check/` is a pure entry point (AST + callable-name set + policy -> findings), never executes, never reaches rpc/ or enforce/, holds no state; invoked once per submission path, and notably NOT on CachedExecutionStart (a registered script is checked at registration, so the hot path pays once); mode read from session/'s live Config per AD-5, never snapshotted; findings reuse port/'s error shape. Explicitly advisory -- passing the check never grants a capability or offsets a budget charge, which stays enforce/'s alone via the Executor (AD-3). The Deferred entry for tree-sitter/LSP was updated to note the language server consumes check/ as a library, which is why the module is pure and daemon-independent. Frontmatter gained `amended:` and FR-26 in binds; spine was already status: final.
- (change) Second post-final amendment same day (2026-09-18), user-directed during sprint-planning follow-up: split the single-crate module tree into a Cargo workspace, one crate per Structural Seed module plus new crates for the language (hexput-ast/lexer/parser/interpreter, never one Structural Seed module themselves -- they sit underneath script/check/plugin) and for tooling (hexput-grammar, hexput-lsp-core). Motivation given by user: "ast parser'ı cli ile ayrı test edebiliriz" (decoupled testability) and "her modülü kendi crate'ine ayıralım... shared crate'in iç modül yapısı da olabildiğince parçalı olmalı" (maximal parceling, with one hexput-shared crate for cross-cutting diagnostics/wire/ids/budget types, itself split into submodules rather than a grab-bag file). Follow-up message added a second constraint: "bütün crateler lib olmalı, binarylerin çıkacağı bin diye ayrı bir crate olmalı, fakat o crate kendi içinde sistematik parçalı olsun" -- every crate is a lib, exactly one crate (hexput-bin) is allowed to produce binaries, and that crate stays a thin src/bin/*.rs wrapper per binary with all real logic in the corresponding lib crate (hexput-daemon, hexput-cli-core, hexput-lsp-core).
- (decision) Framed the crate split as a genuine architectural strengthening, not just a build-layout preference: several ADs (AD-1, AD-3, AD-4, AD-5, AD-8) state "only X may reach Y directly" rules that previously lived in code review under one crate's module tree. Under the crate graph the same rule becomes Cargo's own dependency graph -- e.g. AD-3's "no path reaches enforce/ except through exec/" is now literally true because only the hexput-exec crate lists hexput-enforce as a dependency; hexput-rpc, hexput-plugin, hexput-script physically cannot import it. Documented the full dependency edge list and five concrete AD-to-compile-error mappings (AD-1, AD-3, AD-4, AD-5, AD-8) in the new "Crate dependency graph" subsection so this isn't just asserted but traceable. 22 crates total: hexput-shared, 5 language crates (ast/lexer/parser/interpreter/check) + hexput-cli-core, 12 daemon crates (transport/port/session/connection/script/plugin/globalvar/exec/enforce/rpc/config/daemon), hexput-grammar, hexput-lsp-core, and the single hexput-bin binary-producing crate.
