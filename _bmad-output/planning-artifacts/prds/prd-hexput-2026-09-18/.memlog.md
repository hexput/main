---
topic: Hexput v2 PRD
updated: 2026-09-18T10:36
---

- (decision) Working mode: coaching path. Entry point: Vision + Features (capability-first), fitting Hexput as a dev-infra runtime service rather than a consumer/UX product.
- (decision) Primary input source is the already-completed product brief at _bmad-output/planning-artifacts/briefs/brief-hexput-2026-09-18/ (brief.md + addendum.md) — problem, solution architecture, deployment model, scope in/out, and risks are treated as settled and will not be re-elicited.
- (decision) No static config file. Each backend sends its config as part of connection/registration handshake (init); reconnects do not resend it (config persists with the connection/client-id state). Config is runtime-editable and can be overridden per-execution. Remaining operational details (health checks, metrics/logging shape) delegated to PM authoring by explicit user request.
- (decision) Sandboxing model confirmed: no OS-level sandbox layer, permanently (not deferred, unlike master-slave/rollback which are just not-in-v2). The interpreted language itself is the sandbox — scripts have exactly the access explicitly granted via capability registration and nothing else by construction, not via an added isolation layer.
- (decision) Client SDKs targeted for v2 launch: JavaScript, Node.js, Python, Rust, Go (all at initial release, not phased).
- (decision) v2 phasing: master-slave scaling and rollback registration are out of v2 entirely (deferred, per brief). OS-level sandboxing is out permanently, not phased in later. No further internal phasing (e.g. v2.0/v2.1 split) requested — single release scope.
- (decision) Full tech stack provided by user: Rust, Tokio async runtime, Unix Domain Socket (local), TCP+TLS or WebSocket (tokio-tungstenite) for remote, MessagePack (rmp-serde)+serde wire format, custom restricted interpreter (Rhai-like at first) with AST/bytecode precompiled and cached (moka or DashMap-backed cache), bidirectional capability-based RPC, systemd/Docker deployment, Criterion + tracing/metrics for benchmarking. Per PRD discipline this is implementation detail -> goes to addendum.md, not the PRD body.
- (event) Drafted prd.md (Vision, Target User/JTBD/UJs, Glossary, 7 features with FR-1..FR-12, Non-Goals, MVP Scope, Cross-Cutting NFRs, Open Questions, Assumptions Index) and addendum.md (tech stack, config handshake notes, PM-authored observability proposal, SDK sequencing note). Presenting to user for review before Finalize.
- (change) FR-10 (Client SDKs) split into two phases: Phase 1 launch = JavaScript + Python; Phase 2 = Node.js, Rust, Go. Resolves the earlier [NOTE FOR PM] about 5 simultaneous SDKs being too much scope for a solo project.
- (decision) Resolved Open Question #2: reconnect via Client ID WILL have protection against replay/unauthorized reconnect (not just accepted risk) — adds a new FR under Connection Lifecycle.
- (decision) Resolved Open Question #4: config schema is primarily execution-policy defaults — granular per-execution-request feature toggles (e.g. disabling loop constructs, disabling if/else branching) alongside resource budget defaults, not just numeric limits. Overridable per execution per FR-3.
- (decision) New feature added: developer tooling deliverable for v2 — a tree-sitter grammar for the Hexput language and a simple LSP (language server), to support editor integration/syntax highlighting and basic authoring assistance for script authors.
- (event) Applied all four changes to prd.md/addendum.md: FR-10 SDK phasing (JS+Python then Node.js/Rust/Go), new FR-13 reconnect protection, config schema clarified as execution-policy defaults incl. feature toggles, new §4.8 Developer Tooling (FR-14 tree-sitter grammar, FR-15 basic LSP). Open Questions list updated to reflect resolutions. Presenting to user for further review.
- (event) Finalize: ran input reconciliation (vs brief) and rubric review subagents. Reconciliation found: Named Pipe transport dropped from PRD despite being in brief scope; OS-level-sandboxing framed as permanent in PRD vs 'decide later' in brief (later user clarification in PRD discovery actually settled this as permanent, so PRD is right and brief is stale); rate-limiting/fuzzing/broader RPC-audit-logging from brief addendum not reflected; brief's 'no timeline, active pruning needed' risk not carried to PRD Open Questions. Rubric review found (high): Vision's Rhai-benchmark success criterion has no formal Success Metrics section despite being deferred past MVP shipping bar; plus 3 low/mechanical findings (FR-13 out of sequence, missing [NOTE FOR PM] tag on §4.7, JS vs Node.js SDK split ambiguity).
- (decision) Triage resolved: (1) Named Pipe/Windows transport was a drafting omission, not a scope cut -- restore to FR-9/Glossary/MVP Scope. (2) OS-level sandboxing is PERMANENTLY out, confirmed again with rationale: the project's whole purpose is built-in language-level sandboxing; OS-level sandboxing is not optimized enough for the millions-of-requests/sec target. Brief.md will be updated to match (was still framed as 'decide before deployment', now stale). (3) Rate-limiting/fuzzing/broad RPC-audit-logging hardening ideas stay a brief-addendum-only reference note in PRD addendum, not new PRD FRs/NFRs -- scope intentionally not expanded.
- (event) Applied all triage fixes: restored Named Pipe/Windows transport (Glossary, FR-9, §4.5, MVP scope); added new §7 Success Metrics (SM-1..SM-3, SM-C1..SM-C2) reconciling Vision's Rhai-benchmark claim with §6.2's deferral, renumbering Cross-Cutting NFRs to §8, Open Questions to §9, Assumptions Index to §10; added [NOTE FOR PM] tags on §4.6 (JS vs Node.js SDK ambiguity) and §4.7; added FR-numbering-order clarifying note under §4; added Open Questions #5 (JS/Node.js SDK differentiation) and #6 (no-timeline scope-pruning ownership); added addendum.md reference-only section for brief's hardening backlog (rate limiting, fuzzing, broader RPC audit, unsafe-audit gate) with no new FR/NFR commitments. Also synced brief.md: OS-level sandboxing reframed from 'decide later' to 'permanently out' with rationale (built-in language sandboxing is the whole premise; OS-level conflicts with millions-of-req/sec target), added frontmatter note documenting the correction.
- (event) Ran bmad-review polish (structure + prose lenses) on prd.md and addendum.md, applied all fixes: gave Open Questions stable IDs (OQ-1..OQ-6) and fixed every §8-vs-§9 cross-reference; added FR-10 note to Assumptions Index; disambiguated the 'see addendum' pointer in §8 Performance NFR to point at the brief's addendum explicitly; condensed §5 Non-Goals to cross-reference §2.2 instead of repeating; fixed §6.2's stray Wasmtime/OPA mention to just reference the brief's plan; unified the capability-denied error name across FR-3/FR-6/FR-7; fixed FR-13's 'three Transports' to 'four'; simplified FR-9's stacked parentheticals; converted all ## and ### headings in prd.md to sentence case (Microsoft style guide); fixed remaining Backend/Transport capitalization and Client-ID-grammar inconsistencies; translated the Turkish delegation quote inline; added missing Named Pipe to addendum.md's tech stack; condensed addendum.md's SDK sequencing note to drop the restated Phase 1/2 split already in FR-10.
- (event) PRD finalized
- (change) User requirement added post-finalize: message processing must be fully asynchronous, and incoming execution requests must not block each other (no head-of-line blocking across or within connections). Adding: new FR-16 under §4.2 Script Execution Model, new Concurrency subsection under §8 Cross-Cutting NFRs, and an addendum implementation note (per-connection Tokio task, non-serialized dispatch).
- (event) Applied async/non-blocking requirement: new FR-16 (Non-blocking concurrent execution) under §4.2, new Concurrency subsection under §8 Cross-Cutting NFRs, MVP Scope §6.1 updated, addendum.md gained a Concurrency model implementation note (per-connection Tokio task, async dispatch, CPU-budget as the anti-starvation backstop). brief.md gained a matching Core mechanics bullet. Small, well-scoped amendment post-finalize -- did not re-run the full parallel-subagent reviewer gate, applied directly and self-checked for consistency with existing FR numbering/cross-references. PRD stays status:final.
- (decision) Major new capability added post-finalize: a second registration mode, Plugin Registration, alongside the existing stateless Direct/Cached Execution mode (§4.2). A Plugin is source containing a 'plugin { name = ..., ... }' backend-defined metadata block, top-level Global Variable declarations (persistent, shared state -- deliberately contrasts with FR-5's 'no state bleed' guarantee for stateless Cached Execution, which stays true for that mode only), and one or more handler functions annotated @Event(<name>). Example syntax supplied by user, captured verbatim in addendum.
- (decision) Plugin lifecycle: state persists for the lifetime of the Backend's Client ID -- survives reconnect (since Client ID already carries state per FR-2), torn down on disconnect-without-reconnect or explicit unregister. Event names are Backend-defined free strings (like Registered Functions), except BackendRegisteredInit which is a reserved lifecycle event invoked once at registration before the plugin accepts other events. Global Variable locking: default is per-top-level-key locking (DashMap-like -- mutating one key doesn't block sibling keys); Backend sets the default strategy (safe vs unsafe/lock-free) at registration time, AND may additionally allow Plugin code itself to override the strategy for a specific variable -- both configuration surfaces confirmed by user (not either/or).
- (question) Open questions raised by Plugin Registration that user has not yet answered: (1) must a Backend declare upfront which Event names it may fire against a Plugin, or can any name be fired ad hoc; (2) when multiple handlers bind to the same Event name, is invocation order defined; (3) can one Backend connection register multiple distinct Plugins concurrently, and is Plugin 'name' unique per-connection or daemon-wide; (4) what happens to in-flight Event invocations on Backend disconnect. Adding as OQ-7..OQ-10 rather than guessing.
- (event) Applied Plugin Registration & Events to prd.md: revised Vision (2nd paragraph) and UJ-1 to reflect the two-mode model accurately; added Glossary terms Plugin, Plugin Registration, Event, Global Variable, Global Variable Locking; added new §4.9 feature with FR-17 (registration as distinct mode), FR-18 (reserved BackendRegisteredInit), FR-19 (backend-defined events, ties to existing capability/budget model), FR-20 (Global Variable state/locking, cross-plugin state explicitly out of scope), FR-21 (lifecycle tied to Backend connection); added to MVP Scope §6.1; extended §8 Concurrency NFR; broadened SM-2; added OQ-7..OQ-10 and matching Assumptions Index entry. addendum.md gained a Plugin Global Variable locking implementation note (DashMap-like, shared code path for safe/unsafe modes, per-variable override granularity).
- (event) Self-reviewed prd.md/addendum.md end-to-end after the Plugin Registration addition (Vision, UJ-1, Glossary x5, new §4.9 with FR-17..FR-21, MVP Scope, Concurrency NFR, SM-2, OQ-7..OQ-10, Assumptions Index, addendum implementation note) -- internally consistent, no full reviewer-subagent swarm re-run given proportionality to a single well-scoped feature addition.
- (decision) OQ-7 resolved: event names AND each event's declared return-object shape MUST be defined upfront at Plugin registration time -- not ad hoc. Firing an event later must match a declared name/shape.
- (decision) OQ-8 resolved: handler ordering for handlers bound to the same event is controllable via @Event(<name>, priority = N), gated by whether the Backend's config permits Plugin-level ordering control. Separately, @Event(<name>, async = true) makes a handler run concurrently without waiting for ordering at all; async defaults to false. When async=true, the handler's default Global Variable mutation strategy is unsafe/lock-free (distinct from the Backend's general FR-20 default), unless the Backend explicitly configures async handlers to use the safe/mutex strategy too -- backend-controlled either way.
- (decision) OQ-10 resolved: on Backend disconnect, an in-flight event invocation is NOT immediately cancelled -- the daemon tries to run it to completion locally. It is only cancelled at the point the handler triggers a Remote Function call (RPC back to the Backend) and disconnection is detected there.
- (decision) OQ-9 resolved as a PM call per explicit user delegation ('geri kalan seyleri de kesinlestir'): a single Backend connection CAN register multiple distinct Plugins concurrently; Plugin 'name' is unique per Backend Client ID, not daemon-wide, consistent with different Backends being independent/mutually untrusted (brief's Deployment model). Flagged as PM-authored, not user-confirmed line-by-line.
- (event) Applied all four resolutions to prd.md: FR-19 rewritten (events + return shapes declared upfront, not ad hoc); new FR-22 (priority-based handler ordering, backend-gated) and FR-23 (async handlers bypass ordering, default-unsafe locking) added and physically reordered after FR-21 for readability (IDs stay as assigned); FR-20 gained an async-tied locking consequence; FR-21 gained disconnect-handling consequences (complete locally, cancel only on next RPC attempt); FR-17 gained the PM-decided multi-plugin/name-uniqueness note. OQ-7..OQ-10 marked RESOLVED inline (struck through, not deleted, per the doc's ID-stability convention) with pointers to the resolving FRs; new OQ-11 covers the remaining low-risk [ASSUMPTION]s (priority tie-break, default order, async+priority precedence). Assumptions Index updated. addendum.md gained FR-22/23 concurrency-model implementation notes and an Event-declaration schema note. Updated the §4.9 example to show priority/async syntax.
- (change) Change surfaced during architecture work: SS5 Non-Goals 'Hexput will not ship or require a static configuration file for the daemon' was too broad -- contradicts a new System Config file (operational/daemon-level: transport binds, TLS cert paths, log level, Session TTL default) the user introduced during architecture elicitation. Narrowing the Non-Goal to per-backend/business-logic config (matching Glossary Config and FR-1's original scoping) and adding System Config as a new Glossary term + FR. Also introduces a Session concept (state that outlives the transient connection, with a TTL) refining FR-21's teardown trigger from immediate to TTL-based.
- (event) Applied System Config / Session fix to prd.md: narrowed SS5 Non-Goal to per-backend Config only; added Glossary terms Session and System Config; new FR-24 (System Config, SS4.1); FR-21 revised to TTL-based Session teardown instead of immediate; SS4.1 Description updated; OQ-4 marked RESOLVED; MVP Scope SS6.1 updated.
- (change) New FR-25 needed: Global Variable Behavior (lifetime/scoping strategy per-variable: forever/ttl/separate_each_trigger/keyed), distinct from FR-20's locking strategy. Surfaced during architecture elicitation. 'Keyed' mode's key source is unspecified -- adding as new open question.
- (decision) Keyed Global Variable behavior resolved: the key is supplied by the Backend at Event-fire time (not derived from params implicitly, not from Client ID). Whether a given Event requires a key is declared per-Event by the Plugin/Backend at registration; firing that event without the required key is a defined error, not a silent fallback.
- (event) Applied FR-25 (Global Variable Behavior: forever/ttl/separate_each_trigger/keyed, with keyed-mode key source resolved) to prd.md Glossary, SS4.9, and MVP Scope.
- (change) Refinement to FR-21/FR-2: with multiple concurrent Connections per Session (FR-2), the Session's TTL clock only starts once the last attached Connection drops (zero remaining) -- not on each individual disconnect while sibling Connections are still live.
