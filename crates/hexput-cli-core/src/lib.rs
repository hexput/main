//! Eval + check command logic (Stories 1.9-1.10) for the `hexput` CLI binary.
//! Depends on hexput-lexer, hexput-parser, hexput-interpreter, hexput-check.
//! Exposes `run()`, called by the thin `hexput` binary in hexput-bin.
//! Binds no Architecture Decision directly.

/// Entry point for the `hexput` CLI binary's eval + check subcommands.
///
/// Stub: real argument/subcommand handling lands with Stories 1.9-1.10.
pub fn run() {
    todo!("eval + check CLI commands land with Stories 1.9-1.10")
}
